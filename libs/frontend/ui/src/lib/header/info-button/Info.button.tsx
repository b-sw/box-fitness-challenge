import { InfoOutlineIcon } from '@chakra-ui/icons';
import { IconButton } from '@chakra-ui/react';

export const InfoButton = () => {
    return <IconButton aria-label={'Info'} icon={<InfoOutlineIcon />} />;
};
